[pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = -v --tb=short --strict-markers
markers =
    integration: Integration tests requiring full pipeline
    e2e: End-to-end tests from upload to validation
